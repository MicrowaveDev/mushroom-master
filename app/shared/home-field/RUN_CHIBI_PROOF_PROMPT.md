# Run Home Field Chibi Proof

Use this file as the canonical instruction set for the next chibi generation run.

## Short Launcher Prompt Template

Use this launcher only when the queue item and method gate are allowed, or when the message that gives the launcher also supplies one concrete allowed unblock input such as an explicit paid fallback env file, supplied local proof source PNGs outside `docs/reference`, or a different queue-approved reference-capable generation/editing method. Do not give this as a "new production-ready run" prompt while the queue item is blocked and no unblock input is available; report the method-gate blocker instead.

```text
In /Users/microwavedev/workspace/microwave-hub/mushroom-master, run `npm run game:home-field:generation-queue -- --id=thalla-stage1-chibi-proof` and follow the printed agent instructions exactly.
```

The launcher prompt should stay this short only after the prompt-issuance gate passes. Do not put evolving art-direction or workflow requirements only in the pasted prompt; persist them below, in `app/shared/home-field/home-field-prompts.json`, in `app/shared/home-field/home-field-generation-queue.json`, and in any helper/test surface required by `docs/home-field-imagegen-requirements.md`. The queue command must print the run title, canonical doc, prompt-issuance gate, built-in imagegen default/blocked path, method-gate status, env rules, reference-input gates, local state-sheet source mode, stop gates, and final-response fields needed by a fresh agent. The built-in section must include the `view_image` reference-input staging step, same-context built-in `image_gen` call, built-in preflight flags, and save/claim-after-render instruction so the pasted launcher does not repeat them. The default launcher is built-in/imagegen skill first when the method gate allows it; the API helper is an explicit paid fallback only when built-in/imagegen output is unavailable and `OPENAI_IMAGEGEN_API_KEY` plus `HOME_FIELD_IMAGEGEN_SKILL_UNAVAILABLE=1` are provided. If using a different supplied local proof-source path, replace only the capability-path sentence. Do not set `HOME_FIELD_CHIBI_LOCAL_IMAGE_INPUTS` to the checked-in PNGs under `docs/reference/home-field/`; those are style/reference images, not proof source inputs. If the concrete unblock input is exactly one supplied complete `8x4` local state-sheet PNG, the fresh run must keep `HOME_FIELD_CHIBI_LOCAL_IMAGE_INPUTS` set for preflight/archive, run `npm run game:home-field:stage-chibi-local-source`, skip reference imagegen, verify/audit the derived reference proxy and staged state sheet, then continue with split/candidate/evidence.

## Required Flow

1. Generate only `thalla`. Do not generate Lomie, Axilin, Kirt, Morga, or Dalamar in Stage 1.
2. Read `docs/home-field-imagegen-requirements.md`, then immediately run `npm run game:home-field:generation-queue -- --id=thalla-stage1-chibi-proof`, `npm run game:home-field:chibi-proof-context`, and preflight the intended image path before spawning sub-agents, reading the full document stack, archiving stale files, or attempting candidate/reference imagegen. Prefer built-in `image_gen` only when `HOME_FIELD_BUILTIN_IMAGEGEN_CAN_SAVE=1` and `HOME_FIELD_BUILTIN_IMAGEGEN_CAN_USE_REFERENCES=1` are set after confirming both capabilities from the same agent context that will run imagegen. For the built-in path, load the checked-in reference PNGs with `view_image` only as the current imagegen skill's same-context input-staging step, then call built-in `image_gen` in that same context with those visible images explicitly named as references. Use the paid API fallback only with `npm run game:home-field:preflight-chibi-proof -- --env-file=<explicit-env-file>` after built-in/imagegen output is unavailable; the fallback env file must contain `OPENAI_IMAGEGEN_API_KEY` and `HOME_FIELD_IMAGEGEN_SKILL_UNAVAILABLE=1`. Plain `OPENAI_API_KEY` is ignored for Home Field image generation. Do not infer `.env` or neighboring repo env files. If `HOME_FIELD_CHIBI_LOCAL_IMAGE_INPUTS` points to one complete `8x4` local state-sheet PNG outside `docs/reference`, preflight may pass through that concrete source path; after archive, run `npm run game:home-field:stage-chibi-local-source` and do not run reference imagegen for that local-source path. If preflight fails only because built-in imagegen disk output is unconfirmed and reference-image input binding is already confirmed, run the documented one-shot diagnostic probe from `docs/home-field-imagegen-requirements.md`: one tiny non-candidate built-in `image_gen` render in the same agent context, then `npm run game:home-field:find-imagegen-output -- --since-minutes=5`, with `--include-temp` for only one bounded retry. Count only a file newer than the probe start. If found, rerun preflight with HOME_FIELD_BUILTIN_IMAGEGEN_CAN_SAVE=1 plus HOME_FIELD_BUILTIN_IMAGEGEN_CAN_USE_REFERENCES=1. Passive `view_image`, text prompt references, and filesystem paths are not enough unless the following built-in `image_gen` call uses those visible images as actual inputs. Listing filesystem paths to checked-in PNGs is also not enough. If preflight fails for any other reason, if the diagnostic file is not found, or if reference-image input binding is unavailable, stop without archiving stale files, running an output probe, or using imagegen. Before the final blocker report, still run the read-only `npm run game:home-field:next-chibi-proof` helper so the current scoped prompt, reference-image list, and method gate are captured. The proof requires an actual PNG file on disk; a chat-visible built-in `image_gen` render is not enough. After each generation, save the file directly to the documented `.agent/home-field-workspace/` path or claim it with `npm run game:home-field:claim-imagegen-output -- --since=<render-start-iso> --dest=<documented-path> --verify=<reference|state-sheet>`.
3. After preflight passes, use sub-agents for separate roles: Producer, Validator, Visual Critic, and Handoff. The agent that actually runs imagegen must be in a context with confirmed discoverable PNG output and reference-image input binding; do not assume a sub-agent can use or recover built-in imagegen files unless that capability is proven for that run.
4. Read `docs/home-field-chibi-candidate-contract.md`, `docs/home-field-chibi-regression-ledger.md`, `docs/home-field-chibi-style-reference.md`, `docs/home-field-runtime-asset-contract-plan.md`, the embedded bitmap references at `docs/reference/home-field/chibi-style-agent-log-reference.png`, `docs/reference/home-field/chibi-thalla-liked-2026-06-23.png`, and `docs/reference/home-field/chibi-thalla-previous-best-2026-06-26-state-sheet.png`, `docs/home-field-agent-flow.md`, `app/shared/home-field/home-field-prompts.json`, and `docs/design-requirements.md`.
5. Use `npm run game:home-field:next-chibi-proof` to print the current scoped prompt, paths, commands, copyable sprite-box reference prompt, and required reference-image list. Before imagegen, state the chibi palette plan in the run notes: `12-18` artist-visible colors, fewer than `20` total design colors excluding transparency and `#ff00ff`, with shared cap/robe/skin/gold ramps instead of many local beige, cream, blush, glow, or gold shades. Also state the style-preservation plan: keep the compact grouped-sheet proportions and charm of the 2026-06-26 previous-best reference while fixing its palette bloat/ornament; do not overcorrect into hard pixel art, clean vector/cel icon art, or a large anime/fashion turnaround. State the scale/face/biology plan too: the reference must read as tiny `96x96` source-sprite views for `64px` output, with small dark seed/dot eyes, no glossy anime eye rendering, and no human hair/wig under a mushroom hat. State the status-simplification plan too: authority must read through cap silhouette, robe blocks, posture, and `1-2` flat mycelium/spore marks only, not royal/regalia language, crown jewels, forehead gems, brooches, medallions, pendants, jewelry-like cap crests, gold filigree, scalloped collars, ornamental robe borders, decorative trim clusters, sleeve cuff trim, clasps, collar jewels, or repeated gold badges.
6. Archive stale rejected Thalla state sheets/raw frames, stale reference sheets, and stale Thalla candidate output only after preflight passes. For paid API fallback, run `npm run game:home-field:archive-stale-chibi-proof -- thalla --env-file=<explicit-env-file>` with the same fallback env file. For built-in/local proof-source paths, run the archive helper under the same confirmed capability environment that made preflight pass. The helper must print a manifest under `.agent/home-field-workspace/rejected/`. Do not delete evidence and do not reuse the rejected tiny beige/pixel-sprite raw frames.
7. Current method gate: after rollout `codex-019f1eb1-1027-7752-95cf-d4f37cb0041c`, do not run the queue-backed built-in same-context reference-staging path again unchanged. That path used same-context `view_image` staging of all three reference PNGs, a same-context built-in `image_gen` call that explicitly used the visible images as references, and save/claim handoff, but still produced a `1448x1086` oversized turnaround with blobs up to `249x332` and `91` significant exact colors. The built-in staging method is exhausted for unchanged Thalla proof runs. Continue only with a different reference-capable generation/editing method, supplied local proof source PNGs outside `docs/reference`, or explicit user-approved fallback; otherwise stop after `next-chibi-proof` and report this blocker instead of burning another same-prompt attempt. Do not give another "new production-ready run" launcher prompt in that blocked state unless the prompt includes one concrete allowed unblock input. If the unblock input is a supplied complete `8x4` local state sheet, run `npm run game:home-field:stage-chibi-local-source` instead of reference imagegen; the helper copies the source to `.agent/home-field-workspace/raw/thalla_chibi.states.source.png`, derives `.agent/home-field-workspace/reference/thalla_chibi_turnaround.reference.png` as a non-production reference proxy for evidence, and writes `.agent/home-field-workspace/review/thalla-local-state-sheet-source.manifest.json`. Then run the same reference proxy verifier/palette audit and state-sheet verifier/palette audit before splitting. For paid API fallback, use `npm run game:home-field:chibi-reference-api-proof -- --env-file=<explicit-env-file>` instead of hand-rolling env discovery, venv setup, prompt copying, imagegen call, normalization, verifier, palette audit, and blocker-note writing; this fallback requires `OPENAI_IMAGEGEN_API_KEY` and `HOME_FIELD_IMAGEGEN_SKILL_UNAVAILABLE=1`. The helper extracts the exact copyable reference prompt from `next-chibi-proof`, calls `image_gen.py edit` with the three checked-in PNGs as real `--image` inputs, writes an API-size source PNG, normalizes it to the `512x384` sprite-box source before verifier/audit, and fails if the reference verifier or `--fail-on-bloat` palette audit fails. `chibi-reference-api-proof` creates only the non-production reference sheet; it is not a production-ready state-sheet generator. Do not treat normalization as style approval; the palette audit and visual gate must still pass. If using a different reference-capable path, generate one non-production sprite-box reference sheet first using the exact copyable reference prompt printed by `npm run game:home-field:next-chibi-proof` only after that method gate is satisfied; do not hand-compose or add extra style terms. This must be image-guided, not another text-only reference attempt: attach or same-context stage `docs/reference/home-field/chibi-thalla-previous-best-2026-06-26-state-sheet.png`, `docs/reference/home-field/chibi-thalla-liked-2026-06-23.png`, and `docs/reference/home-field/chibi-style-agent-log-reference.png` as actual image inputs before calling imagegen, and use the roles printed by the helper. If the current imagegen path cannot attach or use those checked-in PNGs as actual same-context image inputs to the generation call, stop and report that image-guided generation is required. Save the result to `.agent/home-field-workspace/reference/thalla_chibi_turnaround.reference.png`; if built-in imagegen writes outside the repo, claim it with `npm run game:home-field:claim-imagegen-output -- --since=<render-start-iso> --dest=.agent/home-field-workspace/reference/thalla_chibi_turnaround.reference.png --verify=reference`. Then run `npm run game:home-field:verify-chibi-proof-files -- --reference` and `npm run game:home-field:palette-audit -- .agent/home-field-workspace/reference/thalla_chibi_turnaround.reference.png --out=.agent/home-field-workspace/review/thalla-reference-palette-audit.json --swatch=.agent/home-field-workspace/review/thalla-reference-palette-swatch.png --fail-on-bloat`. It should show Thalla in `down`, `up`, `left`, and `right` directions as tiny source-sprite views inside invisible `96x96` boxes, with the same proportions, top-down 2.5D camera, limited palette, and detail budget, and at least `70%` of the sheet left as empty `#ff00ff`. Do not fill quadrants or draw enlarged character-turnaround figures. This reference is for consistency only; do not slice it into final frames.
8. Stop and visually inspect the saved reference sheet and palette audit before final frame generation. If there is no saved file or no `thalla-reference-palette-audit.json` plus swatch PNG, the reference gate has not passed. If the reference sheet already looks like pixel art, a tiny doll sprite, a generic elf, a portrait sticker, a human with a mushroom hat or hair under a mushroom cap, a large anime/fashion/showcase turnaround with earrings/jewelry, glossy anime eyes, eyelashes, royal regalia, crown jewel, forehead gem, brooch, chest medallion, pendant, jewelry-like cap crest, scalloped collar, ornamental robe border, decorative trim clusters, sleeve cuff trim, repeated gold badges, watercolor/painterly cap texture, or a different renderer than the style reference, discard it and regenerate before creating final frames.
   - Also fail the reference gate if it already looks like a large soft illustration palette, appears to exceed roughly `20` visible design colors, or uses many near-duplicate beige, cream, blush, glow, or gold tones. Judge visible design swatches, not incidental antialiasing pixels.
   - If two exact-prompt image-guided reference attempts fail for the same palette/style/status-ornament reason, stop and report the repeated reference-gate blocker instead of spending more blind retries. Continue only after changing the persisted prompt/helper or after explicit user direction. Do not fall back to text-only reference attempts; rollout `codex-019f105b-b55a-7ad0-9f8d-38903fdf7999` proved that text-only prompts still produce oversized anime/turnaround art, rollout `codex-019f140b-07a4-7e10-85e1-f64c9d8a0bdb` proved the old image-guided turnaround prompt still produced large painterly turnaround figures, and rollout `codex-019f1482-8954-7b52-9f75-b377cf957645` proved viewing references before a sprite-box text prompt is still not enough.
   - If built-in imagegen renders in chat during proof art generation but the file path is unknown, immediately run `npm run game:home-field:claim-imagegen-output -- --since=<render-start-iso> --dest=<documented-path> --verify=<reference|state-sheet>`. Use `--include-temp` only for one bounded retry. Count only a file newer than that render. If no file is found, stop; do not continue with a deterministic fallback drawing.
9. Generate the final states as ONE coherent `8x4` state sheet for Thalla only through a reference-capable image path with the passed `.agent/home-field-workspace/reference/thalla_chibi_turnaround.reference.png` attached as an actual image input, then save it to `.agent/home-field-workspace/raw/thalla_chibi.states.source.png`; if built-in imagegen writes outside the repo, claim it with `npm run game:home-field:claim-imagegen-output -- --since=<render-start-iso> --dest=.agent/home-field-workspace/raw/thalla_chibi.states.source.png --verify=state-sheet`. For supplied complete local state-sheet source mode, this file is already staged by `npm run game:home-field:stage-chibi-local-source`; do not regenerate it, and continue directly to verifier, palette audit, split, and candidate production. If the active imagegen path can only use prompt text for the grouped state sheet, stop and report that production-ready generation is still blocked instead of spending another unguided attempt. Row order is `down`, `up`, `left`, `right`; columns `0-1` are idle and columns `2-7` are the walk loop. For each row, idle column `0` is the normal planted pose and idle column `1` is a little `1-3px` bob/squish that loops back to normal. It must remain a standing pose with nearly the same visible height; do not make a crouch, seated pose, or deep squat. If the run asks for retina/source quality, a larger proportional `8x4` state sheet is allowed, but it must downscale cleanly to the current `64x64` runtime frames. Do not generate the final idle/walk states as separate imagegen calls; this is the current core rule for state tiles so style and details stay consistent. Keep the same limited palette plan from the reference; do not let the grouped state sheet add painterly gradients or local highlight/shadow tones. Do not generate pixel art, a tiny beige doll sprite, a generic elf, a straight portrait sticker, or a hard flat cel/vector sheet. Do not bake a ground shadow, foot oval, floor contact patch, or blob/cast shadow into any chibi frame; the renderer uses a separate shared chibi shadow layer.
10. Run `npm run game:home-field:verify-chibi-proof-files -- --state-sheet`, then `npm run game:home-field:palette-audit -- .agent/home-field-workspace/raw/thalla_chibi.states.source.png --out=.agent/home-field-workspace/review/thalla-state-sheet-palette-audit.json --swatch=.agent/home-field-workspace/review/thalla-state-sheet-palette-swatch.png --fail-on-bloat`, then split with `npm run game:home-field:split-chibi-state-sheet -- --chroma-key=#ff00ff --resize`.
11. Run `npm run game:home-field:verify-chibi-proof-files -- --frames` before producing the candidate sheet.
12. Produce with `npm run game:home-field:produce-chibi-candidate -- thalla --resize --chroma-key=#ff00ff`. Do not use `--resize-nearest` for production candidates; it preserves hard pixel stair-steps and repeats the rejected style failure.
13. Run `npm run game:home-field:verify-chibi-proof-files -- --candidate`, then `npm run game:home-field:palette-audit -- .agent/home-field-workspace/candidates/chibi-active-roster/latest/web/public/home-field/characters/thalla/spritesheet.png --out=.agent/home-field-workspace/review/thalla-candidate-palette-audit.json --swatch=.agent/home-field-workspace/review/thalla-candidate-palette-swatch.png --fail-on-bloat`. If alpha/halo validation fails from recoverable chroma fringe only, run `npm run game:home-field:recover-chibi-alpha -- thalla` instead of trying tolerance values by hand; do not use it to paper over style, pose, identity, or baked-shadow failures.
14. Validate and build evidence using the candidate root:

```bash
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:validate -- --ids=thalla --check-files --check-review
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:validate -- --ids=thalla --check-files --check-alpha-halo
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:validate -- --ids=thalla --check-files --check-readability
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:validate -- --ids=thalla --check-files --check-runtime-readiness
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:validate -- --ids=thalla --check-files --check-chibi-animation
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:validate -- --ids=thalla --check-files --check-chibi-quality
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:sheet
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:mobile-readability-sheet -- --ids=thalla
HOME_FIELD_ASSET_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest npm run game:home-field:alpha-sheet -- --ids=thalla
npm run game:home-field:chibi-reference-api-proof -- --env-file=<explicit-env-file>
npm run game:home-field:palette-audit -- .agent/home-field-workspace/reference/thalla_chibi_turnaround.reference.png --out=.agent/home-field-workspace/review/thalla-reference-palette-audit.json --swatch=.agent/home-field-workspace/review/thalla-reference-palette-swatch.png --fail-on-bloat
npm run game:home-field:palette-audit -- .agent/home-field-workspace/raw/thalla_chibi.states.source.png --out=.agent/home-field-workspace/review/thalla-state-sheet-palette-audit.json --swatch=.agent/home-field-workspace/review/thalla-state-sheet-palette-swatch.png --fail-on-bloat
npm run game:home-field:palette-audit -- .agent/home-field-workspace/candidates/chibi-active-roster/latest/web/public/home-field/characters/thalla/spritesheet.png --out=.agent/home-field-workspace/review/thalla-candidate-palette-audit.json --swatch=.agent/home-field-workspace/review/thalla-candidate-palette-swatch.png --fail-on-bloat
HOME_FIELD_CANDIDATE_ROOT=.agent/home-field-workspace/candidates/chibi-active-roster/latest HOME_FIELD_CANDIDATE_IDS=thalla npm run game:home-field:candidate-evidence
npm run game:home-field:chibi-candidate-preview
npm run game:home-field:record-chibi-verdict -- thalla --verdict=needs_regen --reason-file=<visual-critic-reason.txt>
```

## Stage 1 Visual Target

Stage 1 proves the character design plus a simple idle/walk read, not full animation polish. Use the sprite-box reference sheet to keep the four directions consistent. Then create one coherent `8x4` final state sheet and split it into 32 isolated character-only frames: 2 idle frames plus 6 simple walk-loop frames for down, up, left, and right. The two idle frames are `normal -> little 1-3px bob/squish`, returning to normal in the runtime loop. The bob/squish must be present in the grouped state sheet itself; do not create it after split by shifting, squashing, stretching, repainting, or otherwise changing frame pose/silhouette. Each direction must have a non-duplicated idle pair and at least 3 unique walk frames in the composed sheet. The validator rejects idle frame 1 if it squats too deeply by losing too much visible height, moving its alpha center too far, or dropping the cap/body too low.

Thalla must read at `64px` on mobile as an ancient gold-white mushroom-elf field-sprite leader with visible mushroom-elf biology, strong simple silhouette, grounded feet/base over the shared separate chibi shadow layer, dark warm outline, and restrained bone/gold/white/brown palette. Generate with a limited sprite palette: `12-18` artist-visible colors across the source sheet, fewer than `20` total design colors excluding transparency and chroma-key, with shared cap/robe/skin/gold ramps instead of many near-duplicate beige, cream, blush, glow, or gold tones. Reference sheets must keep compact source-sprite occupancy: prefer a `512x384` or smaller source sheet, keep each major visible character blob within the verifier's `128x128px` tolerance for the `96x96` sprite-box contract, and reject a `1536x1024` showcase canvas with four enlarged figures even if magenta coverage is high. Represent authority through cap silhouette, robe blocks, posture, and `1-2` flat mycelium/spore marks only; do not use royal regalia, crown jewels, forehead gems, brooches, chest medallions, pendants, jewelry-like cap crests, gold filigree, ornamental robe borders, decorative trim clusters, clasps, collar jewels, or repeated gold badges. Quality bar: in the composed field preview, the chibi must look at least as crisp, contrasted, and finished as the approved bushes, mushrooms, and gates. Avoid a blurry/downscaled sticker look; use chunky readable shapes, a thicker warm-brown outline, clear face/cap/robe value separation, and a stronger tiny silhouette. Use `docs/home-field-chibi-style-reference.md` for the desired squat field-sprite feel and BJD-inspired chibi doll simplicity, including the liked 2026-06-23 Thalla reference as a positive face/cap/robe direction and the 2026-06-26 previous-best state sheet as the positive compact grouped-sheet direction. Fix the previous-best sheet's palette bloat, ornament, and sticker softness without losing its compact charm. Do not copy any reference image and do not use huge white portrait eyes. The rejected beige/pixel-art Thalla candidate is a negative example because it is too generic; the 2026-06-20 Stage 1 candidate is too ornate; the stopped 2026-06-28 rerun is a negative example for overcorrecting into a large flat anime/fashion turnaround with earrings and then baking foot ovals into the grouped sheet. The stopped 2026-06-28 reference-gate rerun after the prompt tightening is a negative example for letting "sovereign/regal/sacred" prompt words become crown-gem, chest-medallion, ornate trim, and painterly texture drift. The stopped 2026-06-29 reference runs are negative examples for treating passively viewed references plus prompt text as enough; they still produced large painterly or oversized reference figures instead of tiny sprite-box views. A soft beige low-contrast chibi is also a negative result even when it passes alpha/readability.

Do not treat mechanical success as style success. Passing sheet mapping, alpha, dimensions, and mobile readability only means the file is usable evidence. It is still a failed chibi candidate if the composed clean preview does not match the hand-drawn elevated 2.5D field-sprite style or if the raw source, alpha edge safety, anchor/footing, separate-shadow policy, or runtime-scale read fails the runtime asset contract. Do not create or commit deterministic/mechanical fallback chibi art unless the user explicitly asks for diagnostic scaffolding; it must never be reported as a fresh imagegen art candidate.

Use an elevated top-down 2.5D hub-sprite view: the top of the mushroom cap/head should be visible, the body should feel planted on the map, and the pose should feel like a small walkable-field character rather than a front-facing portrait sticker. Keep the design much simpler than character art: broad shapes first, `2-3` main color regions, fewer than `20` visible design colors, smooth BJD-like face planes, rounded cheeks, tiny mouth/nose, mitten-like hands, tiny planted feet, `1-2` large gold mycelium/spore marks total, and no ornate filigree, scattered cap freckles, particle halo, jewelry clusters, lace-like micro-detail, oversized eye treatment, painterly gradients, airbrushed blush, or many local highlight/shadow tones. Avoid human-with-hat reads, baked shadows, baked backgrounds, text, UI, floor planes, glossy doll-photo rendering, and loose glow effects that hide the body.

If the reference sheet looks good but the final grouped state sheet drifts from it, regenerate the grouped state sheet. If one cell is defective but the grouped sheet is otherwise strong, a targeted single-cell imagegen repair may be used only after explicitly noting why it will not break style consistency. Deterministic post-split processing may clean alpha/chroma fringe, crop, and resize only; it must not alter pose, motion, silhouette, style, or identity. If any split raw frame contains multiple sprites, a border, a background, crop artifacts, missing idle bob, or detail that disappears into noise at mobile scale, regenerate or repair before producing the candidate sheet.

## Final Response Requirement

The final response must include:

- verdict for `thalla`
- exact workflow and validation commands run and pass/fail, including `npm run game:home-field:next-chibi-proof`; if the run stops at the reference gate, explicitly say grouped state sheet, split frames, candidate production, candidate evidence, preview, and `record-chibi-verdict` were not run
- any recovered validator failure, including failed command, recovery action, and final passing command/log
- sprite-box reference sheet path
- for supplied local state-sheet source runs: source path/hash, local-source provenance manifest, and a note that reference imagegen was skipped while a deterministic reference proxy was derived
- grouped state sheet and split-frame evidence from `candidate-evidence.manifest.json`
- palette/style verdict: intended visible color budget/ramps; `thalla-reference-palette-audit.json`, `thalla-state-sheet-palette-audit.json`, and `thalla-candidate-palette-audit.json` PASS/WARN/FAIL summaries when those stages ran; whether palette bloat appeared in the reference, grouped state sheet, or composed preview; and whether the result preserved the 2026-06-26 previous-best compact charm without overcorrecting into flat cel/pixel/anime styling
- screenshot/evidence paths
- candidate folder link: `/Users/microwavedev/workspace/microwave-hub/mushroom-master/.agent/home-field-workspace/candidates/chibi-active-roster/latest`
- note that no app-facing PNG was overwritten
