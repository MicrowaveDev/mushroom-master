// Compatibility bridge for existing Mushroom imports.
// The implementation now lives in backpack-game-core.

export {
  defaultRectangleShape,
  getBagShape,
  getEffectiveShape,
  getEffectiveDimensions,
  isCellInShape,
  normalizeRotation,
  rotateShape,
  shapeArea
} from '@microwavedev/backpack-game-core';
